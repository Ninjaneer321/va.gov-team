name: "ADE+ Testing (Internal)"
description: "Structured ADE+ accessibility testing template with progress tracking"
title: "[ADE+] <product/feature> — Testing"
labels:
  - "ADE"
  - "accessibility"
assignees:
  - "lakper"
  - "sara-amanda"

body:
  - type: markdown
    attributes:
      value: |
        > [!IMPORTANT]
        > This is the Issue Form version of the ADE+ internal testing template.
        > - Select **Pass / Fail / N/A** for each assistive technology (AT) for every test.
        > - Use the **Findings / notes** section to document any failures.
        > - (Optional automation) Any **Fail** selection can be used to create bug tickets.

  - type: input
    id: product_feature
    attributes:
      label: Product / Feature
      placeholder: "e.g., 10-10EZR online form"
    validations:
      required: true

  - type: input
    id: product_team
    attributes:
      label: Product Team
      placeholder: "e.g., Health Care Enrollment"
    validations:
      required: true

  - type: input
    id: test_date
    attributes:
      label: Test date (YYYY-MM-DD)
      placeholder: "2026-03-17"
    validations:
      required: true

  - type: input
    id: testers
    attributes:
      label: Tester(s)
      placeholder: "@username1, @username2"
    validations:
      required: true

  - type: textarea
    id: environments
    attributes:
      label: Test environments (AT / Browser / OS / Version)
      description: "List the combinations you tested. Example: JAWS 2024 + Chrome 122 + Windows 11"
      placeholder: |
        - No AT (keyboard only): Chrome 122 / macOS 14
        - JAWS: JAWS 2024 / Chrome 122 / Windows 11
        - iOS VoiceOver: iOS 17 / Safari
